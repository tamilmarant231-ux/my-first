#include <stdio.h>
int countDigits(int N)
{
    int count=0;
    while (N>0)
    {
        N/=10;   
        count++;  
    }
    return count;
}
int main()
{
    int N;
    scanf("%d",&N);
    int totalDigits=countDigits(N);
    printf("%d\n",totalDigits);
    return 0;
}