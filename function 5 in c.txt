#include <stdio.h>
int digitSum(int N)
{
    int sum=0;
    while (N>0)
    {
        sum+=N%10; 
        N/=10;       
    }
    return sum;
}
int main()
{
    int N;
    scanf("%d",&N);
    int sum=digitSum(N);
    printf("%d\n",sum); 
    return 0;
}