#include <stdio.h>
int sumOfNumbers(int N)
{
    int sum=0;
    for(int i=1;i<=N;i++)
    {
        sum+=i;
    }
    return sum;
}
int main()
{
    int N;
    scanf("%d",&N);
    int total=sumOfNumbers(N);
    printf("%d\n",total);
    return 0;
}