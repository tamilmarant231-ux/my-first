#include <stdio.h>
int main() {
    int a,b;
    scanf("%d %d",&a,&b);
    if(b>a)
    {
        printf("20 is greater");
    }
    else
    {
        printf("false");
    }
    return 0;
}