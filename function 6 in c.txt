#include <stdio.h>
long long factorial(int N)
{
    long long fact=1;
    for (int i=1;i<=N;i++)
    {
        fact*=i;
    }
    return fact;
}
int main(){
    int N;
    scanf("%d",&N);
    long long result=factorial(N);
    printf("%lld\n",result);  
    return 0;
}